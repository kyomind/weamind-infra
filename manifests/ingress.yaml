apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: weamind
  namespace: weamind
  annotations:
    # Tell Traefik to apply the HTTPS redirect middleware before routing.
    traefik.ingress.kubernetes.io/router.middlewares: weamind-https-redirect@kubernetescrd
spec:
  tls:
    - hosts:
        - k8s.kyomind.tw
      secretName: k8s-kyomind-tw-tls
  ingressClassName: traefik
  rules:
    - host: k8s.kyomind.tw
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: weamind-line-bot
                port:
                  number: 80
