# 2026-03-31 Secret Update And Encoding Debug Story QA

> 原則：今天不把 Secret 當抽象名詞背誦，而是回到 WeaMind 的 Deployment、Secret 寫法與真實 debug 記錄。

## Q1

### 題目

請你先對照 `manifests/deployment.yaml` 說明：WeaMind 目前是怎麼把 Secret 注入 Pod 的？

除了指出 `secretRef` 之外，還要一起說明這代表什麼語意：這是逐個 key 指定，還是整包匯入？它和 Day 1 的 `env + valueFrom` 邊界差在哪裡？

### 對照檔案

- `manifests/deployment.yaml`
- `.privatedocs/secrets/secret.yaml`
- `docs/WeaMind Infra核心架構.md`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始

## Q2

### 題目

如果今天更新了 Secret 或 ConfigMap，為什麼既有 Pod 不一定會自動拿到新值？

這題不要只回答「因為 Kubernetes 不會自動重啟 Pod」，而是要再往下講一層：WeaMind 目前是用什麼方式把設定送進 container，所以更新資源本身和更新 container 內的環境變數，為什麼不是同一件事？

### 對照檔案

- `manifests/deployment.yaml`
- `PROGRESS.md`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始

## Q3

### 題目

請你把 WeaMind 這次 `CreateContainerError (invalid UTF-8)` 的 debug story 重新講一遍。

至少要包含：當時的症狀是什麼、錯在哪一層、真正根因是什麼、最後怎麼修，以及這次事件後 repo 收斂出了什麼規則。

### 對照檔案

- `PROGRESS.md`
- `.privatedocs/weamind/踩坑清單.md`
- `.privatedocs/secrets/secret.yaml`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始

## Q4

### 題目

假設你今天修改了 `LINE_CHANNEL_SECRET` 或 `POSTGRES_PASSWORD`，但 app 行為看起來完全沒變。你第一輪會怎麼排查？

請不要直接丟指令清單，而是先用判斷順序回答：你會先確認什麼、再確認什麼、最後什麼情況下才會合理地做 `rollout restart`？

### 對照檔案

- `manifests/deployment.yaml`
- `PROGRESS.md`
- `.privatedocs/weamind/踩坑清單.md`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始
