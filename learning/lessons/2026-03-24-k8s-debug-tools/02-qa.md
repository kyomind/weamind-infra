# 2026-03-24 K8s Debug Tools QA

> 原則：今天的重點不是多背幾條 `kubectl` 指令，而是知道每個工具比較像在拿哪種證據，並把它放回 WeaMind 的真實 debug 情境。

## Q1

### 題目

如果只用一句話先區分 `kubectl describe pod`、`kubectl logs`、`kubectl logs --previous`、`kubectl exec -it`，你會怎麼描述它們各自最適合看的東西？請不要只背指令名稱，而是直接說「它比較像在拿哪種證據」。

### 對照檔案

- `manifests/deployment.yaml`
- `PROGRESS.md`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始

---

## Q2

### 題目

如果今天看到 Pending、ImagePullBackOff、CreateContainerError、CrashLoopBackOff，這四種狀態在第一輪通常比較適合先開哪一種工具？請不要追求唯一正解，而是說明你為什麼先選那個工具，以及你期待先拿到什麼證據。

### 對照檔案

- `manifests/deployment.yaml`
- `README.md`
- `PROGRESS.md`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始

---

## Q3

### 題目

把工具放回 WeaMind 的真實案例來看：如果你遇到 Hetzner LB health check 未帶 Host header 導致 `/health` 回 `404`，以及 LINE webhook 路徑一開始設成 `/webhook` 導致 `404`，你會優先選哪些工具或觀察點？為什麼這兩個案例都不該第一步就直接 `exec` 進 Pod？

### 對照檔案

- `PROGRESS.md`
- `docs/LINE-Webhook-切換流程.md`
- `manifests/ingress.yaml`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始

---

## Q4

### 題目

`kubectl exec -it` 在 WeaMind 這個專案裡最適合拿來驗證哪些事情？又有哪些事情即使你 `exec` 進 Pod 了，也不能因此直接下結論說外部流量路徑沒問題？

### 對照檔案

- `manifests/configmap.yaml`
- `manifests/service.yaml`
- `README.md`

### 使用者回答摘要

- 待回答

### AI 修正與補充

- 待補

### 狀態

- 未開始
